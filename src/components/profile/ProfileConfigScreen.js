import React from 'react'

/**
 * Functional component que retorna la página de configuración del perfíl de usuario
 * @returns {DOMElement}
 */

export const ProfileConfigScreen = () => {
    return (
        <div>
            <h1>configuracion</h1>
        </div>
    )
}
